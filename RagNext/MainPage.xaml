<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:RagNext"
             xmlns:controls="clr-namespace:RagNext.Views.Controls"
             x:Class="RagNext.MainPage"
             Title="Game Editor">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="File" Clicked="OnFileMenuClicked" />
        <ToolbarItem Text="Edit" Clicked="OnEditMenuClicked" />
        <ToolbarItem Text="Settings" Clicked="OnSettingsMenuClicked" />
        <ToolbarItem Text="Help" Clicked="OnHelpMenuClicked" />
    </ContentPage.ToolbarItems>

    <!-- Wrap existing content -->
    <controls:RightPaneLayout>
        <controls:RightPaneLayout.Main>
            <ScrollView>
                <VerticalStackLayout Padding="30,20" Spacing="16">
                    <Image Source="dotnet_bot.png" HeightRequest="140" Aspect="AspectFit"
                           SemanticProperties.Description="dot net bot in a hovercraft number nine" />
                    <Label Text="Edit Game" FontSize="20" HorizontalOptions="Center" />

                    <Border StrokeShape="RoundRectangle 8" StrokeThickness="0" Background="#FFF" Padding="12">
                        <Border.Shadow>
                            <Shadow Radius="8" Opacity="0.35" Brush="Black" Offset="0,4" />
                        </Border.Shadow>
                        <VerticalStackLayout Spacing="12">
                            <Label Text="Title" />
                            <Entry Text="{Binding Title, Mode=TwoWay}" Placeholder="Enter game title" />
                            <Label Text="Author" />
                            <Entry Text="{Binding Author, Mode=TwoWay}" Placeholder="Enter author name" />
                            <Label Text="Version" />
                            <Entry Text="{Binding Version, Mode=TwoWay}" Placeholder="e.g. 1.0.0" />
                        </VerticalStackLayout>
                    </Border>

                    <HorizontalStackLayout Spacing="12" HorizontalOptions="Fill">
                        <Button x:Name="CounterBtn" Text="Current Game: None"  HorizontalOptions="StartAndExpand" />
                        <Button x:Name="Savebtn" Text="Save Game" Clicked="OnSaveClicked"
                                SemanticProperties.Hint="Saves the game" HorizontalOptions="End" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </ScrollView>
        </controls:RightPaneLayout.Main>
    </controls:RightPaneLayout>
</ContentPage>
