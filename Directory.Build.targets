<Project>
  <!-- Set linker to SdkOnly for Mac Catalyst — avoids over-stripping app assemblies -->
  <PropertyGroup Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'maccatalyst'">
    <MtouchLink>SdkOnly</MtouchLink>
  </PropertyGroup>
</Project>
